---
phase: 35-cross-project-bridges-backfill
plan: 01
type: execute
wave: 1
depends_on: [34]
files_modified:
  - src/services/memory/knowledge-graph/graph.ts
  - src/services/memory/types.ts
  - src/services/memory/backfill/project-scan.ts
  - src/services/memory/store.ts
  - src/services/memory/service.ts
  - src/mcp/tools/graph-tools.ts
autonomous: true
requirements:
  - BRIDGE-01
  - BRIDGE-02
  - BRIDGE-03

must_haves:
  truths:
    - "graph_bridges() returns non-zero bridges when memories exist across 2+ nests with shared KG entities"
    - "graph_bridges() works even when kg_entities.memory_id is NULL, by falling back to source_memory_id on triples"
    - "graph_bridges() response includes an insights[] array with human-readable cross-project pattern descriptions"
    - "graph_bridges() response includes a summary string counting bridges, nest pairs, and shared entities"
    - "localnest_project_backfill({ root_path }) scans for projects and creates seed memories for those with zero entries"
    - "localnest_project_backfill({ root_path, dry_run: true }) returns discovered projects without writing"
    - "Backfill scan ignores node_modules, .git, dist, build, target, __pycache__, .venv directories"
    - "Backfill scan detects projects via package.json, Cargo.toml, go.mod, pyproject.toml, pom.xml, .git presence"
    - "Backfill-created memories have source_type 'project_scan' and auto-tagged"
    - "Existing graph_bridges callers still work (backward compat -- new fields are additive)"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/services/memory/knowledge-graph/graph.ts"
      provides: "Enhanced discoverBridges with LEFT JOIN fallback and insight generation"
      exports: ["discoverBridges"]
      contains: "LEFT JOIN"
    - path: "src/services/memory/types.ts"
      provides: "Extended DiscoverBridgesResult with insights[] and summary, new ProjectBackfill types"
      contains: "insights"
    - path: "src/services/memory/backfill/project-scan.ts"
      provides: "scanAndBackfillProjects function for filesystem project discovery"
      exports: ["scanAndBackfillProjects"]
      contains: "package.json"
    - path: "src/services/memory/store.ts"
      provides: "scanAndBackfillProjects method on MemoryStore"
      contains: "scanAndBackfillProjects"
    - path: "src/services/memory/service.ts"
      provides: "scanAndBackfillProjects method on MemoryService"
      contains: "scanAndBackfillProjects"
    - path: "src/mcp/tools/graph-tools.ts"
      provides: "localnest_project_backfill MCP tool registration"
      contains: "localnest_project_backfill"
  key_links:
    - from: "src/mcp/tools/graph-tools.ts (localnest_project_backfill)"
      to: "src/services/memory/backfill/project-scan.ts (scanAndBackfillProjects)"
      via: "MCP tool delegates to backfill function"
      pattern: "scanAndBackfillProjects"
    - from: "src/services/memory/knowledge-graph/graph.ts (discoverBridges)"
      to: "src/services/memory/types.ts (DiscoverBridgesResult)"
      via: "Returns enriched result with insights"
      pattern: "insights"
---

<objective>
Enable cross-project pattern discovery and backfill. Three changes: (1) fix `graph_bridges` to return real data when `kg_entities.memory_id` is NULL by using LEFT JOINs with `source_memory_id` fallback, plus add `insights[]` and `summary`; (2) new `localnest_project_backfill` MCP tool that scans a directory for projects with zero memories and creates seed entries; (3) enriched bridge output with human-readable cross-project insight strings.

Purpose: Today `graph_bridges` returns zero results because INNER JOINs on `memory_id` drop auto-created entities. After this phase, bridges surface reliably, agents can backfill empty projects, and cross-project insights are explicit.

Output: Modified graph.ts, types.ts, new project-scan.ts, modified store.ts + service.ts + graph-tools.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-cross-project-bridges-backfill/35-CONTEXT.md
@src/services/memory/knowledge-graph/graph.ts
@src/services/memory/knowledge-graph/auto-link.ts
@src/services/memory/types.ts
@src/services/memory/store.ts
@src/services/memory/service.ts
@src/mcp/tools/graph-tools.ts
@src/services/memory/store/entries.ts

<interfaces>
From types.ts (existing -- to be extended):
```typescript
export interface DiscoverBridgesOpts { nest?: string; }
export interface BridgeEntry {
  triple_id: string; subject_id: string; subject_name: string;
  predicate: string; object_id: string; object_name: string;
  subject_nest: string; object_nest: string;
}
export interface DiscoverBridgesResult {
  filter_nest: string | null; bridge_count: number; bridges: BridgeEntry[];
}
```

From graph.ts (existing discoverBridges SQL -- INNER JOINs are the bug):
```sql
JOIN memory_entries ms ON ms.id = s.memory_id   -- drops NULL memory_id
JOIN memory_entries mo ON mo.id = o.memory_id   -- drops NULL memory_id
WHERE ms.nest != mo.nest
```

From store.ts (wiring pattern for new methods):
```typescript
async discoverBridges(args: DiscoverBridgesOpts) {
  await this.init();
  const hookResult = await this.hooks.emit('before:graph:bridges', args);
  if (hookResult.cancelled) return { cancelled: true, reason: hookResult.reason };
  const result = await discoverBridgesFn(this.adapter!, hookResult.payload);
  await this.hooks.emit('after:graph:bridges', result);
  return result;
}
```

From store/entries.ts:
```typescript
export async function storeEntry(store: MemoryStoreLike, input: StoreEntryInput): Promise<StoreEntryResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix discoverBridges query and add insights (BRIDGE-01, BRIDGE-03)</name>
  <files>src/services/memory/types.ts, src/services/memory/knowledge-graph/graph.ts</files>
  <behavior>
    BRIDGE-01 -- Fix SQL to find bridges when memory_id is NULL:
    - Replace INNER JOINs with LEFT JOINs. Add a third LEFT JOIN on `memory_entries mt ON mt.id = t.source_memory_id`.
    - Derive nest via COALESCE: `COALESCE(NULLIF(ms1.nest,''), NULLIF(mt.nest,''))` for subject, `COALESCE(NULLIF(mo1.nest,''), NULLIF(mt.nest,''))` for object.
    - Wrap in a subquery, filter outer WHERE on `subject_nest != '' AND object_nest != '' AND subject_nest != object_nest`.
    - Also SELECT `s.entity_type AS subject_type` and `o.entity_type AS object_type` for insight generation.

    BRIDGE-03 -- Post-query insight generation:
    - Group bridges by entity ID. For each entity appearing in 2+ nests, produce: `"'name' (type) bridges nestA, nestB via 'predicate'"`.
    - Compute summary: `"Found N bridge(s) across M nest pair(s) involving K shared entity/entities"`.

    Type changes:
    - Add `subject_type?: string` and `object_type?: string` to BridgeEntry.
    - Add `insights: string[]` and `summary: string` to DiscoverBridgesResult.
  </behavior>
  <action>
    1) In types.ts, extend BridgeEntry with `subject_type?: string; object_type?: string;`. Extend DiscoverBridgesResult with `insights: string[]; summary: string;`.

    2) In graph.ts, update BridgeRow to include `subject_type: string; object_type: string;`.

    3) Rewrite discoverBridges function body:
       - Build SQL with LEFT JOINs (ms1 via s.memory_id, mo1 via o.memory_id, mt via t.source_memory_id).
       - Use subquery to derive subject_nest/object_nest via COALESCE.
       - After fetching rows, build entityNests Map grouping entities by ID with their nests and predicates.
       - Generate insights[] from entities spanning 2+ nests.
       - Compute nestPairs Set and sharedEntityCount for summary.
       - Return extended DiscoverBridgesResult with bridges, insights, summary.
  </action>
</task>

<task type="auto">
  <name>Task 2: Project backfill scan (BRIDGE-02)</name>
  <files>src/services/memory/backfill/project-scan.ts (new), src/services/memory/types.ts, src/services/memory/store.ts, src/services/memory/service.ts</files>
  <behavior>
    New file project-scan.ts (under 200 lines):
    - Exports `scanAndBackfillProjects(store, opts)`.
    - Scans up to 2 levels deep under `rootPath` for project markers.
    - SKIP_DIRS: node_modules, .git, dist, build, target, __pycache__, .venv, .next, .cache, coverage.
    - MARKERS: package.json (ts/js), Cargo.toml (rust), go.mod (go), pyproject.toml/setup.py/requirements.txt (python), pom.xml/build.gradle/build.gradle.kts (java/kotlin), .git-only fallback (unknown).
    - For each project: check `COUNT(*) FROM memory_entries WHERE nest = ?`. Skip if > 0.
    - If dry_run, record status 'would_backfill'. Otherwise call storeEntry with title=basename, content=description, nest=basename, branch='project-info', tags=['auto:project','auto:{lang}'], source_type='project_scan', importance=40.
    - Returns ProjectBackfillResult with counts and per-project details.

    Type additions in types.ts:
    - ProjectBackfillOpts: `{ rootPath: string; dryRun?: boolean }`
    - ProjectBackfillProject: `{ path, name, language, marker, status }`
    - ProjectBackfillResult: `{ root_path, projects_found, projects_backfilled, projects_skipped, dry_run, projects[] }`

    Store.ts: Add `scanAndBackfillProjects(opts)` method using dynamic import of project-scan.ts. Emit before/after hooks `before:graph:backfill_projects` / `after:graph:backfill_projects`. Add ProjectBackfillOpts/Result to type imports.

    Service.ts: Add `scanAndBackfillProjects(opts)` delegating to store. Add ProjectBackfillOpts to type imports.
  </behavior>
  <action>
    1) Add ProjectBackfillOpts, ProjectBackfillProject, ProjectBackfillResult types to types.ts after DiscoverBridgesResult.

    2) Create src/services/memory/backfill/project-scan.ts with scanAndBackfillProjects function. Use fs.readdirSync for scanning, fs.existsSync for marker detection. Define MemoryStoreLike interface locally (adapter with get(), storeEntry(), init()). Recursive walk function with depth limit. Non-fatal error handling per project.

    3) Add scanAndBackfillProjects method to MemoryStore in store.ts. Dynamic import to avoid circular deps. Hook emit pattern matching existing methods.

    4) Add scanAndBackfillProjects method to MemoryService in service.ts. Delegates to store.
  </action>
</task>

<task type="auto">
  <name>Task 3: Register localnest_project_backfill MCP tool (BRIDGE-02)</name>
  <files>src/mcp/tools/graph-tools.ts</files>
  <behavior>
    - Add `scanAndBackfillProjects(opts: Record<string, unknown>): Promise<unknown>` to MemoryService interface.
    - Register `localnest_project_backfill` tool after `localnest_graph_bridges`:
      - root_path: z.string().min(1).max(1000) -- REQUIRED
      - dry_run: z.boolean().default(false) -- optional
      - Annotations: readOnly=false, destructive=false, idempotent=true
      - Handler maps root_path -> rootPath, dry_run -> dryRun and calls memory.scanAndBackfillProjects.
  </behavior>
  <action>
    1) Add scanAndBackfillProjects to MemoryService interface in graph-tools.ts.
    2) Add registerJsonTool call for localnest_project_backfill between graph_bridges and diary tools.
  </action>
</task>

</tasks>

<verification>
After execution, verify:

1. **BRIDGE-01**: `graph_bridges` returns bridge_count > 0 when entities span multiple nests (even with NULL memory_id on kg_entities, as long as triples have source_memory_id pointing to memories in different nests).

2. **BRIDGE-02**: `localnest_project_backfill({ root_path: '/path', dry_run: true })` returns projects with 'would_backfill' status. Without dry_run, creates seed memories. Second run skips them ('skipped_has_memories').

3. **BRIDGE-03**: `graph_bridges` response contains `insights: string[]` and `summary: string` with meaningful content.

4. **Backward compat**: Existing bridge_count/bridges[] fields unchanged. New fields additive.

5. **Build**: `npm run build` succeeds.

6. **Tests**: `npm test` passes.
</verification>
