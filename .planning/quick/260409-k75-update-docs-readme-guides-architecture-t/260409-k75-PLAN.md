---
phase: quick
plan: 260409-k75
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - localnest-docs/docs/guides/architecture.md
autonomous: true
requirements: [DOC-UPDATE]
must_haves:
  truths:
    - "All links in README.md point to correct post-restructure paths"
    - "Architecture doc source layout reflects the new subdirectory structure"
    - "CONTRIBUTING.md references updated to new localnest-docs paths"
  artifacts:
    - path: "README.md"
      provides: "Corrected links for architecture, translations, translation policy"
    - path: "localnest-docs/docs/guides/architecture.md"
      provides: "Updated source layout tree and file references"
    - path: "CONTRIBUTING.md"
      provides: "Updated guide references"
  key_links:
    - from: "README.md"
      to: "localnest-docs/docs/guides/architecture.md"
      via: "relative link"
    - from: "README.md"
      to: "localnest-docs/docs/i18n/*.md"
      via: "relative links"
---

<objective>
Update existing docs (README.md, CONTRIBUTING.md, architecture.md) to reflect the folder restructuring:
1. Docs moved from `guides/` and `readme/` to `localnest-docs/docs/`
2. Memory service modules reorganized into subdirectories (events/, ingest/, knowledge-graph/, store/, taxonomy/)

Purpose: Keep documentation accurate after the restructure.
Output: Updated existing files only -- no new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@CONTRIBUTING.md
@localnest-docs/docs/guides/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and CONTRIBUTING.md link paths</name>
  <files>README.md, CONTRIBUTING.md</files>
  <action>
In README.md, update these broken references:

1. Line 19: `./guides/architecture.md` -> `./localnest-docs/docs/guides/architecture.md`
2. Line 23: All `./readme/README.XX-YY.md` links -> `./localnest-docs/docs/i18n/README.XX-YY.md` (13 translation links)
3. Line 25: `./readme/TRANSLATION_POLICY.md` -> `./localnest-docs/docs/i18n/TRANSLATION_POLICY.md`
4. Line 494: `./guides/architecture.md` -> `./localnest-docs/docs/guides/architecture.md`

In CONTRIBUTING.md, update these broken references:

1. Line 50: `./guides/architecture.md` -> `./localnest-docs/docs/guides/architecture.md`
2. Line 51: `./guides/future-retrieval-roadmap.md` -> `./localnest-docs/docs/guides/future-retrieval-roadmap.md`
  </action>
  <verify>grep -c "guides/architecture.md\|readme/README\.\|readme/TRANSLATION" README.md should return 0 (no old paths remain). grep -c "guides/" CONTRIBUTING.md | grep for old paths should return 0.</verify>
  <done>All links in README.md and CONTRIBUTING.md point to correct localnest-docs/docs/ paths. No broken references remain to the old guides/ or readme/ directories.</done>
</task>

<task type="auto">
  <name>Task 2: Update architecture.md source layout and file references</name>
  <files>localnest-docs/docs/guides/architecture.md</files>
  <action>
Update the "Source layout" tree (lines 454-489) to reflect the new subdirectory structure under `src/services/memory/`:

Replace the flat listing:
```
├── memory/
│   ├── schema.ts
│   ├── store.ts
│   ├── service.ts
│   ├── entries.ts
│   ├── recall.ts
│   ├── relations.ts
│   ├── kg.ts
│   ├── graph.ts
│   ├── taxonomy.ts
│   ├── scopes.ts
│   ├── dedup.ts
│   ├── ingest.ts
│   ├── hooks.ts
│   ├── event-capture.ts
│   ├── event-heuristics.ts
│   ├── event-list.ts
│   ├── workflow.ts
│   ├── adapter.ts
│   └── utils.ts
```

With the new subdirectory structure:
```
├── memory/
│   ├── schema.ts        # table DDL + forward-only migrations (v1-v9)
│   ├── store.ts         # MemoryStore facade — wires all modules together
│   ├── service.ts       # MemoryService — backend detection, public API
│   ├── hooks.ts         # MemoryHooks pub-sub system
│   ├── workflow.ts      # high-level workflow helpers
│   ├── adapter.ts       # SQLite adapter abstraction
│   ├── types.ts         # shared type definitions
│   ├── utils.ts         # shared helpers (nowIso, cleanString, stableJson)
│   ├── events/
│   │   ├── capture.ts   # event capture + signal scoring
│   │   ├── heuristics.ts # signal score computation
│   │   └── list.ts      # event listing
│   ├── ingest/
│   │   └── ingest.ts    # conversation ingestion (markdown + JSON)
│   ├── knowledge-graph/
│   │   ├── graph.ts     # recursive CTE traversal + bridge discovery
│   │   ├── kg.ts        # knowledge graph entities + triples
│   │   └── relations.ts # legacy bidirectional relations
│   ├── store/
│   │   ├── dedup.ts     # semantic duplicate detection via cosine similarity
│   │   ├── entries.ts   # CRUD for memory_entries
│   │   └── recall.ts    # recall queries with filtering and ranking
│   └── taxonomy/
│       ├── scopes.ts    # agent diary + agent-scoped isolation
│       └── taxonomy.ts  # nest/branch hierarchy queries
```

Also update inline file references in the prose sections:
- Line 137: "managed by `kg.ts` and `graph.ts`" -> "managed by `knowledge-graph/kg.ts` and `knowledge-graph/graph.ts`"
- Line 139: section heading `(kg.ts)` -> `(knowledge-graph/kg.ts)`
- Line 165: section heading `(graph.ts)` -> `(knowledge-graph/graph.ts)`
- Line 184: "`relations.ts`" -> "`knowledge-graph/relations.ts`"
- Line 190: "managed by `taxonomy.ts`" -> "managed by `taxonomy/taxonomy.ts`"
- Line 215: section heading `(scopes.ts)` -> `(taxonomy/scopes.ts)`
- Line 230: section heading `(dedup.ts)` -> `(store/dedup.ts)`
- Line 253: section heading `(ingest.ts)` -> `(ingest/ingest.ts)`
- Line 285: file path example `src/services/memory/kg.ts` -> `src/services/memory/knowledge-graph/kg.ts`
  </action>
  <verify>grep -c "├── entries.ts\|├── recall.ts\|├── kg.ts\|├── graph.ts\|├── taxonomy.ts\|├── scopes.ts\|├── dedup.ts\|├── ingest.ts\|├── event-capture.ts" localnest-docs/docs/guides/architecture.md should return 0 for flat listing. The new subdirectory entries should appear instead.</verify>
  <done>Architecture source layout tree shows the new events/, ingest/, knowledge-graph/, store/, taxonomy/ subdirectories. All inline file references updated to include subdirectory prefix.</done>
</task>

</tasks>

<verification>
- `grep -rn "./guides/architecture.md\|./readme/" README.md` returns no matches
- `grep -rn "./guides/" CONTRIBUTING.md` returns no matches to old paths
- The source layout in architecture.md shows 5 subdirectories under memory/
- No new files were created
</verification>

<success_criteria>
- README.md: All 16+ broken links updated to localnest-docs/docs/ paths
- CONTRIBUTING.md: 2 guide references updated
- architecture.md: Source layout tree reflects actual directory structure with 5 subdirectories; 9+ inline file references updated with subdirectory prefix
- Zero new files created
</success_criteria>

<output>
After completion, create `.planning/quick/260409-k75-update-docs-readme-guides-architecture-t/260409-k75-SUMMARY.md`
</output>
