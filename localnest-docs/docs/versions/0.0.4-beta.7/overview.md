---
description: Overview of LocalNest v0.0.4-beta.7, the archived beta release covering Hugging Face runtime migration, sqlite-vec install hardening, and package cleanup.
---

# 0.0.4-beta.7 Overview

`0.0.4-beta.7` is an archived beta package line.

<div className="docPanel docPanel--compact">
  <p>
    This archive page freezes the beta.7 documentation contract so teams can reference the exact
    release behavior before beta.8 became current.
  </p>
</div>

## What this version includes

- canonical `localnest_*` tool names
- lexical, semantic, and hybrid retrieval
- opt-in local memory store and workflow tools
- top-level `localnest` command path for setup, doctor, and upgrade
- Hugging Face embeddings and reranking as the current runtime default
- sqlite-vec extension auto-detection for cleaner global installs

## Best use

Use this version archive when your MCP client or deployment is pinned to `localnest-mcp@0.0.4-beta.7` and you want docs aligned exactly to that runtime behavior.
