---
description: Overview of LocalNest v0.0.4-beta.8, the archived beta release covering startup regression fixes, safer sqlite-vec status handling, and release hardening.
---

# 0.0.4-beta.8 Overview

`0.0.4-beta.8` is an archived beta package line.

<div className="docPanel docPanel--compact">
  <p>
    This archive page freezes the beta.8 documentation contract so teams can reference the exact
    release behavior before beta.9 became current.
  </p>
</div>

## What this version includes

- canonical `localnest_*` tool names
- lexical, semantic, and hybrid retrieval
- opt-in local memory store and workflow tools
- top-level `localnest` command path for setup, doctor, and upgrade
- Hugging Face embeddings and reranking as the shipped runtime default
- sqlite-vec extension auto-detection from beta.7 plus safer locked-database status behavior

## Best use

Use this version archive when your MCP client or deployment is pinned to `localnest-mcp@0.0.4-beta.8` and you want docs aligned exactly to that runtime behavior.
