---
description: Overview of LocalNest v0.0.4-beta.9, the archived beta release covering bundled skill version fixes, package metadata hardening, and the beta.8 runtime baseline.
---

# 0.0.4-beta.9 Overview

`0.0.4-beta.9` is an archived beta package line.

<div className="docPanel docPanel--compact">
  <p>
    This archive page freezes the beta.9 documentation contract so teams can reference the exact
    release behavior independently of later documentation changes.
  </p>
</div>

## What this version includes

- canonical `localnest_*` tool names
- lexical, semantic, and hybrid retrieval
- opt-in local memory store and workflow tools
- top-level `localnest` command path for setup, doctor, upgrade, and skill sync
- Hugging Face embeddings and reranking as the shipped runtime default
- bundled skill version reporting fixes on top of the beta.8 runtime baseline

## Best use

Use this version archive when your MCP client or deployment is pinned to `localnest-mcp@0.0.4-beta.9` and you want docs aligned exactly to that runtime behavior.
