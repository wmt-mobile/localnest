---
description: Overview of LocalNest v0.0.4-beta.5 beta docs, including the top-level localnest command path and the published beta behavior baseline.
---

# 0.0.4-beta.5 Overview

`0.0.4-beta.5` is an archived beta package line.

<div className="docPanel docPanel--compact">
  <p>
    This archive page freezes the beta.5 documentation contract so teams can reference the exact
    release behavior after beta.6 became current.
  </p>
</div>

## What this version includes

- canonical `localnest_*` tool names
- lexical, semantic, and hybrid retrieval
- opt-in local memory store and workflow tools
- top-level `localnest` command path for setup, doctor, and upgrade
- bundled skill sync behavior and generated MCP config output

## Best use

Use this version archive when your MCP client or deployment is pinned to `localnest-mcp@0.0.4-beta.5` and you want docs aligned exactly to that runtime behavior.
