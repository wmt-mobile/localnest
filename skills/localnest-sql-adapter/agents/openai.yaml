interface:
  display_name: "LocalNest SQL Adapter"
  short_description: "Work on LocalNest SQLite adapters, migrations, and sqlite-vec loading safely"
  default_prompt: "Use this skill when editing LocalNest SQLite-backed retrieval or memory code. Identify direct node:sqlite imports and constructors first, keep current Node 22 behavior stable, isolate adapter-selection changes from schema/query changes, verify sqlite-vec extension loading explicitly, and test retrieval and memory paths separately."
